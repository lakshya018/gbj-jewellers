export const metadata = {
  title: 'Shopping Cart | GBJ Jewellers',
  description: 'Review your selected jewellery pieces, apply promo codes, and proceed to secure checkout at GBJ Jewellers.',
  robots: { index: false, follow: false },
};

export default function CartLayout({ children }) {
  return children;
}
